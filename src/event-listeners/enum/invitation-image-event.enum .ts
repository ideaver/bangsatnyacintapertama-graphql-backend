export enum InvitationImageEvent {
  CreatedMany = 'invitation-image.created.many',
  Updated = 'invitation-image.updated',
  Deleted = 'invitation-image.deleted',
  Resend = 'invitation-image.resend',
}
